# Руководство по деплою на Railway

## 🔧 Настройки для Railway

### Переменные окружения

В Railway нужно установить следующие переменные окружения:

```
DATABASE_URL=postgresql://user:password@host:port/database
# или
DATABASE_PUBLIC_URL=postgresql://user:password@host:port/database
PORT=3001
```

### Важные моменты:

1. **PORT**: Railway автоматически устанавливает переменную `PORT`, но в коде используется значение по умолчанию 3001, если PORT не указан
2. **DATABASE_URL**: Убедитесь, что база данных PostgreSQL настроена и URL корректный
3. **CORS**: Настроен для работы с любыми доменами (Railway, локально, и т.д.)

## 🔍 Решение проблем

### Проблема: PIN не работает / WebSocket не подключается

**Возможные причины и решения:**

#### 1. **Неправильный URL для подключения**

Railway предоставляет домен вида: `your-app.up.railway.app`

**✅ Правильные URL:**
```javascript
// REST API (HTTP/HTTPS)
const apiUrl = 'https://your-app.up.railway.app';

// WebSocket (ОБЯЗАТЕЛЬНО wss:// для HTTPS!)
const socket = io('wss://your-app.up.railway.app/quiz', {
  transports: ['websocket', 'polling'], // Поддержка обоих транспортов
  upgrade: true,
  rememberUpgrade: true,
});
```

**❌ Неправильные URL:**
```javascript
// ❌ НЕ используйте ws:// на Railway (только для localhost)
const socket = io('ws://your-app.up.railway.app/quiz'); // ❌

// ❌ НЕ используйте http:// для Railway
const apiUrl = 'http://your-app.up.railway.app'; // ❌
```

#### 2. **Проверка подключения к базе данных**

**Если PIN не работает, первым делом проверьте БД:**

```bash
# Проверьте endpoint здоровья БД
curl https://your-app.up.railway.app/health/db
```

**Должен вернуть:**
```json
{
  "status": "connected",
  "message": "База данных успешно подключена",
  "database": "railway"
}
```

**Если возвращает ошибку:**
- Проверьте переменную окружения `DATABASE_URL` в Railway
- Убедитесь, что база данных создана и запущена
- Проверьте логи Railway на наличие ошибок подключения к БД

#### 3. **CORS блокирует запросы**

✅ CORS настроен на разрешение всех источников (`origin: '*'`)

**Если все еще есть ошибки CORS:**
- Убедитесь, что фронтенд использует правильный домен Railway (с `https://`)
- Проверьте консоль браузера для детальных ошибок CORS
- Убедитесь, что не используется `credentials: true` с `origin: '*'` (несовместимо)

#### 4. **Проблемы с WebSocket подключением**

**Симптомы:**
- WebSocket не подключается
- События не приходят/не отправляются
- Таймауты подключения

**Решения:**
- Используйте `wss://` вместо `ws://` (Railway использует HTTPS)
- Убедитесь, что namespace `/quiz` указан правильно
- Проверьте логи Railway - должны быть сообщения `🔌 [Connection] Client connected`
- Используйте оба транспорта: `transports: ['websocket', 'polling']`

### Проверка работы сервера

1. **Проверьте, что сервер запущен:**
   ```
   GET https://your-app.up.railway.app/health/db
   ```
   Должен вернуть статус подключения к БД

2. **Проверьте Swagger документацию:**
   ```
   https://your-app.up.railway.app/api
   ```

3. **Проверьте логи в Railway:**
   - Должны быть логи: `🚀 Приложение запущено на порту ...`
   - При подключении WebSocket должны быть логи: `🔌 [Connection] Client connected`

## 📋 Чек-лист для решения проблем с PIN

### Шаг 1: Проверка базы данных

- [ ] В Railway создана база данных PostgreSQL
- [ ] Переменная окружения `DATABASE_URL` установлена в настройках проекта Railway
- [ ] Проверен endpoint `/health/db` - возвращает `status: 'connected'`
- [ ] В логах Railway есть сообщение: `✅ База данных подключена: <database_name>`

### Шаг 2: Проверка сервера

- [ ] Сервер запущен (в логах: `🚀 Приложение запущено на порту ...`)
- [ ] Нет ошибок при старте сервера
- [ ] Доступен endpoint `/` - возвращает "Hello World!"

### Шаг 3: Проверка URL на фронтенде

- [ ] REST API использует `https://your-app.up.railway.app` (не `http://`)
- [ ] WebSocket использует `wss://your-app.up.railway.app/quiz` (не `ws://`)
- [ ] Namespace `/quiz` указан правильно

### Шаг 4: Проверка WebSocket подключения

- [ ] WebSocket подключается (в логах Railway: `🔌 [Connection] Client connected`)
- [ ] Событие `join-quiz` отправляется и обрабатывается
- [ ] В логах есть сообщения: `👨‍🏫 [Join] УЧИТЕЛЬ подключился` или `👨‍🎓 [Join] УЧЕНИК подключился`

### Шаг 5: Проверка PIN

- [ ] PIN генерируется при создании игры (GET `/teacher/quizzes`)
- [ ] Можно найти игру по PIN (GET `/student/quizzes/pin/:pin`)
- [ ] Можно присоединиться к игре по PIN (POST `/student/quizzes/pin/:pin/join`)

## 🐛 Отладка проблем

### Логи для проверки:

1. **При старте сервера:**
   ```
   ✅ База данных подключена: <database_name>
   🚀 Приложение запущено на порту <port>
   ```

2. **При WebSocket подключении:**
   ```
   🔌 [Connection] Client connected: <socket_id>
   ```

3. **При ошибках:**
   - Проверьте логи на наличие ошибок подключения к БД
   - Проверьте, что все зависимости установлены
   - Проверьте переменные окружения

### Частые ошибки:

1. **"Database connection error"**
   - Проверьте `DATABASE_URL` в Railway
   - Убедитесь, что база данных доступна

2. **"WebSocket connection failed"**
   - Используйте `wss://` вместо `ws://`
   - Проверьте, что URL правильный (Railway домен)

3. **"CORS error"**
   - CORS настроен на разрешение всех источников
   - Если проблема остается, проверьте логи браузера для деталей

## 🔗 Полезные ссылки

- Railway Dashboard: https://railway.app
- Документация Railway: https://docs.railway.app

