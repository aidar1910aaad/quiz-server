# WebSocket API Документация для Фронтенда

## Подключение

```javascript
const socket = io('http://localhost:3001/quiz', {
  transports: ['websocket']
});
```

---

## Последовательность событий для УЧИТЕЛЯ

### 1. Подключение к игре (ОБЯЗАТЕЛЬНО ПЕРВЫМ!)

```javascript
socket.emit('join-quiz', {
  pin: '148161',
  quizId: 'a81e7d21-6768-47f3-9568-18996c1e9e9e',
  userId: '67b495fa-...', // ID пользователя из авторизации
  role: 'teacher' // ОБЯЗАТЕЛЬНО 'teacher'
});
```

**Важно:** 
- Это событие ДОЛЖНО быть отправлено ПЕРВЫМ перед любыми другими действиями
- После переподключения нужно ОБЯЗАТЕЛЬНО отправить `join-quiz` снова
- Бэкенд сохраняет `client.data.role` и `client.data.userId` только после `join-quiz`

**Ответ бэкенда:**
```javascript
socket.on('quiz-state', (data) => {
  // data.quiz.status - статус игры (CREATED, STARTED, FINISHED)
  // data.currentQuestionIndex - текущий индекс вопроса
});
```

**Логи бэкенда:**
```
👨‍🏫 [Join] УЧИТЕЛЬ подключился к игре: PIN=148161, userId=67b495fa..., quizId=..., quizTitle="..."
```

### 2. Запуск игры

```javascript
socket.emit('start-quiz', {
  pin: '148161',
  quizId: 'a81e7d21-6768-47f3-9568-18996c1e9e9e',
  userId: '67b495fa-...' // Тот же userId, что и в join-quiz
});
```

**Ответ бэкенда:**
```javascript
socket.on('game-update', (data) => {
  // data.status - новый статус игры (должен стать 'STARTED')
  // data.currentQuestionIndex - 0 (первый вопрос)
});
```

**Логи бэкенда:**
```
📥 [Event Received] start-quiz: PIN=148161, ...
▶️ [Start Quiz] Учитель запустил игру: PIN=148161, ...
▶️ [Start Quiz] Игра запущена успешно: PIN=148161, totalQuestions=5
```

### 3. Запуск первого вопроса

```javascript
socket.emit('start-question', {
  pin: '148161',
  currentQuestionIndex: 0 // Индекс первого вопроса (начинается с 0)
});
```

**Ответ бэкенда:**
```javascript
socket.on('start-question', (data) => {
  // data.pin - PIN игры
  // data.currentQuestionIndex - индекс вопроса
  // data.questionId - ID вопроса
  // data.timeSeconds - время на ответ в секундах
  // data.timestamp - время запуска
});

socket.on('game-update', (data) => {
  // data.currentQuestionIndex - обновленный индекс вопроса
});
```

**Логи бэкенда:**
```
📥 [Event Received] start-question: PIN=148161, questionIndex=0, ...
📤 [Start Question] Обработка запроса на запуск вопроса: PIN=148161, ...
✅ [Start Question] Вопрос 0 запущен: PIN=148161, questionId=..., таймер=30с + 3с запас = 33с до автоперехода
```

### 4. Автоматический переход к следующему вопросу

**Бэкенд автоматически переходит к следующему вопросу через `timeSeconds + 3 секунды`**

**События от бэкенда:**
```javascript
socket.on('start-question', (data) => {
  // Автоматически приходит событие для следующего вопроса
  // data.currentQuestionIndex - следующий индекс (1, 2, 3...)
});
```

**Логи бэкенда:**
```
⏰ [Backend] Timer expired for question 0 in quiz ...
⏭️ [Auto Advance] Автоматический переход к следующему вопросу: PIN=148161, текущий=0, следующий=1, всего=5
```

### 5. Завершение игры

**Автоматически после последнего вопроса:**
```javascript
socket.on('game-update', (data) => {
  // data.status - 'FINISHED'
});

// Автоматически приходит событие с результатами
socket.on('quiz-results', (results) => {
  // Полная статистика игры (см. раздел "Получение результатов")
});
```

**Логи бэкенда:**
```
🏁 [Auto Advance] Последний вопрос завершен, автоматическое завершение игры: PIN=148161, ...
🏁 [Game Finish] Игра завершена (последний вопрос): PIN=148161, Победитель: 🔴 Красная команда, ...
📊 [Results] Результаты игры отправлены учителю: PIN=148161, team1Score=314, team2Score=299
```

**Или вручную учителем:**
```javascript
socket.emit('finish-quiz', {
  pin: '148161',
  quizId: 'a81e7d21-6768-47f3-9568-18996c1e9e9e',
  userId: '67b495fa-...'
});
```

---

## Последовательность событий для УЧЕНИКА

### 1. Подключение к игре

```javascript
socket.emit('join-quiz', {
  pin: '148161',
  role: 'student',
  participantId: 'participant-uuid', // Получен при присоединении через REST API
  name: 'Иван',
  team: 1 // 1 = Красная команда, 2 = Синяя команда
});
```

**Логи бэкенда:**
```
👨‍🎓 [Join] УЧЕНИК подключился к игре: PIN=148161, participantId=..., name="Иван", team=🔴 Красная
```

### 2. Ожидание запуска вопроса

**Ученик получает событие от бэкенда:**
```javascript
socket.on('start-question', (data) => {
  // data.currentQuestionIndex - индекс вопроса
  // data.questionId - ID вопроса
  // data.timeSeconds - время на ответ
  // Начинаем таймер на фронтенде
});
```

### 3. Отправка ответа

```javascript
socket.emit('submit-answer', {
  pin: '148161',
  questionId: 'question-uuid',
  participantId: 'participant-uuid',
  selectedOptionId: 'option-uuid',
  responseTimeMs: 5000 // Время ответа в миллисекундах
});
```

**Ответ бэкенда:**
```javascript
socket.on('answer-confirmed', (data) => {
  // data.participantId - ID участника
  // data.questionId - ID вопроса
  // data.isCorrect - правильность ответа
  // data.responseTimeMs - время ответа
});

socket.on('tug-position-update', (data) => {
  // data.position - позиция каната (-100 до 100)
  // data.team1Score - очки красной команды
  // data.team2Score - очки синей команды
  // data.hasAnswers - есть ли ответы у команд
});
```

**Логи бэкенда:**
```
📥 [Event Received] submit-answer: PIN=148161, questionId=..., participantId=..., responseTime=5с
📝 [Answer] Ученик ответил: PIN=148161, name="Иван", team=🔴 Красная, questionId=..., responseTime=5с
📝 [Answer] Результат ответа: "Иван" (🔴 Красная) - ✅ ПРАВИЛЬНО, responseTime=5с
🎯 [Tug Position] Обновление позиции каната: PIN=148161, position=15.50, team1Score=327, team2Score=0
```

### 4. Завершение игры и получение статистики

```javascript
socket.on('game-update', (data) => {
  if (data.status === 'FINISHED') {
    // Запрашиваем персональную статистику через REST API
    fetch(`http://localhost:3001/student/quizzes/pin/${pin}/participants/${participantId}/results`)
      .then(res => res.json())
      .then(studentResults => {
        console.log('Моя статистика:', studentResults);
        // Показываем результаты ученику
      });
  }
});
```

---

## Получение результатов игры

### Для учителя (автоматически при завершении игры)

После завершения игры бэкенд **автоматически** отправляет событие `quiz-results` всем в комнате. Учитель должен слушать это событие:

```javascript
socket.on('quiz-results', (results) => {
  console.log('Результаты игры:', results);
  displayResults(results);
});
```

**Или запросить результаты вручную:**
```javascript
socket.emit('get-results', {
  pin: '148161',
  quizId: 'a81e7d21-6768-47f3-9568-18996c1e9e9e',
  userId: '67b495fa-...'
});

socket.on('results', (results) => {
  // Те же данные, что и в quiz-results
  displayResults(results);
});
```

**Структура данных результатов:**

```typescript
{
  quiz: {
    id: string,
    title: string,
    pin: string,
    status: 'finished',
    totalQuestions: number,        // Общее количество вопросов
    totalParticipants: number,     // Общее количество участников
    durationMinutes: number        // Продолжительность игры в минутах
  },
  team1: {
    team: 1,
    participants: [
      {
        id: string,                    // ID участника
        name: string,                  // Имя участника
        correctAnswers: number,        // Количество правильных ответов
        totalAnswers: number,          // Всего ответов
        averageSpeed: number,          // Средняя скорость ответов (сек)
        participantScore: number,      // Баллы участника (правильные × 50 + скорость × 10)
        averageResponseTimeMs: number, // Среднее время ответа в миллисекундах
        answers: [                     // Детали всех ответов
          {
            questionId: string,
            isCorrect: boolean,
            responseTimeMs: number
          }
        ]
      }
    ],
    totalCorrect: number,              // Всего правильных ответов команды
    totalAnswers: number,              // Всего ответов команды
    totalScore: number,                // Общие баллы команды
    averageSpeed: number,              // Средняя скорость команды
    totalParticipants: number          // Количество участников в команде
  },
  team2: {
    // Аналогично team1
    team: 2,
    participants: [...],
    totalCorrect: number,
    totalAnswers: number,
    totalScore: number,
    averageSpeed: number,
    totalParticipants: number
  },
  winner: 1 | 2 | null,               // Победитель (1 = Красная, 2 = Синяя)
  finalPosition: number,               // Финальная позиция каната (-100 до 100)
  tugStatus: {                         // Полный статус каната
    position: number,
    team1Score: number,
    team2Score: number,
    hasAnswers: boolean
  }
}
```

**Пример обработки результатов:**

```javascript
socket.on('quiz-results', (results) => {
  // Общая информация
  console.log(`Игра "${results.quiz.title}" завершена`);
  console.log(`Всего вопросов: ${results.quiz.totalQuestions}`);
  console.log(`Всего участников: ${results.quiz.totalParticipants}`);
  console.log(`Продолжительность: ${results.quiz.durationMinutes} мин`);
  
  // Победитель
  const winnerTeam = results.winner === 1 ? '🔴 Красная команда' : '🔵 Синяя команда';
  console.log(`Победитель: ${winnerTeam}`);
  console.log(`Финальная позиция: ${results.finalPosition.toFixed(2)}`);
  
  // Статистика команд
  console.log(`\n🔴 Красная команда:`);
  console.log(`  Баллы: ${results.team1.totalScore}`);
  console.log(`  Правильных ответов: ${results.team1.totalCorrect}/${results.team1.totalAnswers}`);
  console.log(`  Средняя скорость: ${results.team1.averageSpeed}`);
  console.log(`  Участников: ${results.team1.totalParticipants}`);
  
  // Статистика каждого участника красной команды
  results.team1.participants.forEach(participant => {
    console.log(`\n  👤 ${participant.name}:`);
    console.log(`    Правильных: ${participant.correctAnswers}/${participant.totalAnswers}`);
    console.log(`    Средняя скорость: ${participant.averageSpeed}`);
    console.log(`    Баллы: ${participant.participantScore}`);
    console.log(`    Среднее время ответа: ${Math.round(participant.averageResponseTimeMs / 1000)}с`);
  });
  
  // Аналогично для синей команды
  console.log(`\n🔵 Синяя команда:`);
  // ...
});
```

### Для ученика (REST API)

После завершения игры ученик может получить свою персональную статистику через REST API:

```javascript
// GET /student/quizzes/pin/:pin/participants/:participantId/results
const response = await fetch(
  `http://localhost:3001/student/quizzes/pin/${pin}/participants/${participantId}/results`
);
const studentResults = await response.json();
```

**Структура ответа:**

```typescript
{
  participant: {
    id: string,
    name: string,
    team: 1 | 2
  },
  quiz: {
    id: string,
    title: string,
    pin: string,
    status: 'finished'
  },
  stats: {
    correctAnswers: number,        // Количество правильных ответов
    totalAnswers: number,          // Всего ответов
    accuracy: number,              // Точность в процентах (0-100)
    averageResponseTime: number,   // Среднее время ответа в мс
    participantScore: number,      // Баллы участника
    contribution: number           // Вклад в команду в процентах (0-100)
  },
  teamStats: {
    team: 1 | 2,
    teamScore: number,             // Общие баллы команды
    totalCorrectAnswers: number,   // Всего правильных ответов команды
    totalParticipants: number,     // Количество участников команды
    averageSpeed: number           // Средняя скорость команды
  },
  answers: [
    {
      questionId: string,
      questionText: string,
      selectedOptionText: string,
      isCorrect: boolean,
      responseTimeMs: number,
      answeredAt: string
    }
  ],
  finalPosition: number,           // Финальная позиция каната (-100 до 100)
  winner: 1 | 2 | null            // Победитель (1 = Красная, 2 = Синяя)
}
```

**Пример обработки:**

```javascript
const studentResults = await response.json();

console.log(`Моя статистика: ${studentResults.participant.name}`);
console.log(`Команда: ${studentResults.participant.team === 1 ? '🔴 Красная' : '🔵 Синяя'}`);
console.log(`Правильных ответов: ${studentResults.stats.correctAnswers}/${studentResults.stats.totalAnswers}`);
console.log(`Точность: ${studentResults.stats.accuracy}%`);
console.log(`Мои баллы: ${studentResults.stats.participantScore}`);
console.log(`Вклад в команду: ${studentResults.stats.contribution}%`);
console.log(`Среднее время ответа: ${Math.round(studentResults.stats.averageResponseTime / 1000)}с`);

console.log(`\nСтатистика команды:`);
console.log(`  Баллы команды: ${studentResults.teamStats.teamScore}`);
console.log(`  Правильных ответов: ${studentResults.teamStats.totalCorrectAnswers}`);
console.log(`  Средняя скорость: ${studentResults.teamStats.averageSpeed}`);

console.log(`\nПобедитель: ${studentResults.winner === 1 ? '🔴 Красная команда' : '🔵 Синяя команда'}`);
console.log(`Финальная позиция: ${studentResults.finalPosition.toFixed(2)}`);
```

**Важно:** 
- Статистика доступна только после завершения игры (`status: 'finished'`)
- Если игра еще не завершена, API вернет ошибку 400

---

## Обработка ошибок

### Ошибка: "Игра не запущена"

**Причина:** Попытка запустить вопрос, когда игра в статусе `CREATED` или `FINISHED`

**Решение:**
1. Убедитесь, что сначала отправлен `start-quiz`
2. Дождитесь события `game-update` со статусом `STARTED`
3. Только потом отправляйте `start-question`

### Ошибка: "Только учитель может запустить вопрос"

**Причина:** `client.data.role` не установлен или не равен `'teacher'`

**Решение:**
1. Убедитесь, что ПЕРВЫМ отправлен `join-quiz` с `role: 'teacher'`
2. После переподключения ОБЯЗАТЕЛЬНО отправьте `join-quiz` снова
3. Проверьте, что `userId` в `join-quiz` совпадает с `userId` в `start-quiz`

### Ошибка: "Неверный индекс вопроса"

**Причина:** `currentQuestionIndex` выходит за границы массива вопросов

**Решение:**
1. Отслеживайте `currentQuestionIndex` на фронтенде
2. Не пытайтесь запустить вопрос с индексом >= `totalQuestions`

---

## Важные замечания

1. **Бэкенд - единственный источник правды:** Бэкенд автоматически управляет переходами между вопросами. Фронтенд должен только слушать события `start-question` и реагировать на них.

2. **Таймеры на бэкенде:** Бэкенд сам устанавливает таймеры и автоматически переходит к следующему вопросу. Фронтенд может показывать свой таймер для UX, но реальный переход контролирует бэкенд.

3. **Переподключение:** При переподключении WebSocket:
   - ОБЯЗАТЕЛЬНО отправьте `join-quiz` снова
   - Бэкенд отправит `quiz-state` с текущим состоянием игры
   - Восстановите UI на основе полученного состояния

4. **Логирование:** Все события логируются на бэкенде с префиксами:
   - `📥 [Event Received]` - входящее событие от клиента
   - `👨‍🏫 [Join]` / `👨‍🎓 [Join]` - подключение учителя/ученика
   - `▶️ [Start Quiz]` - запуск игры
   - `📤 [Start Question]` - запуск вопроса
   - `📝 [Answer]` - ответ ученика
   - `🎯 [Tug Position]` - обновление позиции каната
   - `⏭️ [Auto Advance]` - автоматический переход
   - `⏹️ [Finish Question]` - завершение вопроса
   - `🏁 [Game Finish]` - завершение игры
   - `📊 [Results]` - отправка результатов
   - `🧹 [Cleanup]` - очистка неактивных подключений

5. **Автоматическая отправка результатов:** После завершения игры бэкенд автоматически отправляет событие `quiz-results` всем в комнате. Учитель должен слушать это событие для отображения результатов.

---

## Пример полного потока для учителя

```javascript
// 1. Подключение
const socket = io('http://localhost:3001/quiz');

// 2. Присоединение к игре (ОБЯЗАТЕЛЬНО ПЕРВЫМ!)
socket.emit('join-quiz', {
  pin: '148161',
  quizId: 'a81e7d21-6768-47f3-9568-18996c1e9e9e',
  userId: '67b495fa-...',
  role: 'teacher'
});

// 3. Слушаем состояние игры
socket.on('quiz-state', (data) => {
  console.log('Состояние игры:', data.quiz.status);
});

// 4. Запускаем игру
socket.emit('start-quiz', {
  pin: '148161',
  quizId: 'a81e7d21-6768-47f3-9568-18996c1e9e9e',
  userId: '67b495fa-...'
});

// 5. Слушаем обновления игры
socket.on('game-update', (data) => {
  if (data.status === 'STARTED') {
    // Запускаем первый вопрос
    socket.emit('start-question', {
      pin: '148161',
      currentQuestionIndex: 0
    });
  }
});

// 6. Слушаем запуск вопросов (включая автоматические переходы)
socket.on('start-question', (data) => {
  console.log('Вопрос запущен:', data.currentQuestionIndex);
  // Показываем вопрос на экране
  // Запускаем таймер на фронтенде (для UX, реальный таймер на бэкенде)
});

// 7. Слушаем завершение игры и получаем результаты
socket.on('game-update', (data) => {
  if (data.status === 'FINISHED') {
    console.log('Игра завершена!');
  }
});

// 8. Слушаем автоматическую отправку результатов
socket.on('quiz-results', (results) => {
  console.log('Результаты игры:', results);
  // Отображаем результаты
  displayResults(results);
});
```

---

## Пример полного потока для ученика

```javascript
// 1. Подключение
const socket = io('http://localhost:3001/quiz');

// 2. Присоединение к игре
socket.emit('join-quiz', {
  pin: '148161',
  role: 'student',
  participantId: 'participant-uuid',
  name: 'Иван',
  team: 1
});

// 3. Слушаем запуск вопроса
socket.on('start-question', (data) => {
  console.log('Вопрос запущен:', data.currentQuestionIndex);
  // Показываем вопрос
  // Запускаем таймер
});

// 4. Отправляем ответ
socket.emit('submit-answer', {
  pin: '148161',
  questionId: 'question-uuid',
  participantId: 'participant-uuid',
  selectedOptionId: 'option-uuid',
  responseTimeMs: 5000
});

// 5. Слушаем подтверждение ответа
socket.on('answer-confirmed', (data) => {
  console.log('Ответ принят:', data.isCorrect);
});

// 6. Слушаем обновления позиции каната
socket.on('tug-position-update', (data) => {
  console.log('Позиция каната:', data.position);
  // Обновляем визуализацию каната
});

// 7. Слушаем завершение игры
socket.on('game-update', (data) => {
  if (data.status === 'FINISHED') {
    console.log('Игра завершена!');
    // Запрашиваем персональную статистику через REST API
    fetch(`http://localhost:3001/student/quizzes/pin/${pin}/participants/${participantId}/results`)
      .then(res => res.json())
      .then(studentResults => {
        console.log('Моя статистика:', studentResults);
        // Показываем результаты ученику
        displayStudentResults(studentResults);
      });
  }
});
```

---

## Все события WebSocket

### События от клиента к серверу:

- `join-quiz` - Присоединение к игре (учитель/ученик)
- `start-quiz` - Запуск игры (только учитель)
- `start-question` - Запуск вопроса (только учитель)
- `submit-answer` - Отправка ответа (только ученик)
- `finish-question` - Завершение вопроса вручную (только учитель)
- `finish-quiz` - Завершение игры вручную (только учитель)
- `get-results` - Запрос результатов (только учитель)
- `get-tug-position` - Запрос текущей позиции каната
- `get-participants` - Запрос списка участников (только учитель)

### События от сервера к клиенту:

- `quiz-state` - Текущее состояние игры (при присоединении)
- `participant-joined` - Новый участник присоединился
- `participants-update` - Обновление списка участников
- `game-update` - Обновление состояния игры
- `start-question` - Вопрос запущен
- `question-started` - Подтверждение запуска вопроса (устаревшее)
- `question-finished` - Вопрос завершен
- `answer-confirmed` - Ответ принят и обработан
- `tug-position-update` - Обновление позиции каната
- `quiz-results` - Результаты игры (автоматически при завершении)
- `results` - Результаты игры (при запросе через get-results)
- `error` - Ошибка

---

## Формулы расчета баллов

### Баллы участника:
```
participantScore = (correctAnswers × 50) + (averageSpeed × 10)
```

### Средняя скорость участника:
```
averageSpeed = maxTimeSeconds - averageResponseTimeSeconds
```
где `maxTimeSeconds` - среднее время на вопрос из настроек игры

### Баллы команды:
```
teamScore = (totalCorrectAnswers × 50) + (teamAverageSpeed × 10)
```

### Позиция каната:
- От -100 (синяя команда побеждает) до +100 (красная команда побеждает)
- Рассчитывается на основе соотношения баллов команд
- Для достижения ±100 нужна разница баллов 3:1
- Если одна команда имеет 0 баллов, позиция ограничена ±75

---

## Технические детали

### Namespace
Все WebSocket события работают в namespace `/quiz`

### CORS
Бэкенд настроен на прием подключений с любых источников (`origin: '*'`)

### Таймауты
- Неактивные подключения (не присоединившиеся к игре) отключаются через 5 минут
- Автоматический переход к следующему вопросу происходит через `timeSeconds + 3 секунды` (3 секунды на показ результатов)

### Очистка ресурсов
- При отключении клиента все его данные очищаются
- При завершении игры очищаются таймеры и кэш
- При остановке сервера все интервалы очищаются корректно
